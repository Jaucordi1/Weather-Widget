/*
import {ICON_URL, retrieveWeather, WeatherResponse} from "./weather"
import 'geolocation-storage'
import createLoader from "./components/loader"
import createWeatherDisplay from "./components/weather"
import createLocateMeBtn from "./components/locate-me-btn"

export const DEBUG = true

class WeatherWidget {
	private parent: HTMLElement
	private geolocationOptions: Required<PositionOptions> = {
		timeout: 10000,
		enableHighAccuracy: true,
		maximumAge: 20 * 1000
	}
	private _geolocator: PositionSubscriber | undefined
	private weatherTimer: number | undefined
	private _initiated: boolean = false
}
*/
